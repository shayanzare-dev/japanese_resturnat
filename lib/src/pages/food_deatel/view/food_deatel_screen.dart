import 'package:flutter/material.dart';

class FoodDeatelScreen extends StatelessWidget {
  const FoodDeatelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Food Deatel screen'),
      ),
    );
  }
}
